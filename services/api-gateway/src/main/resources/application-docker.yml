spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true # Включаем автоматическое обнаружение сервисов
          lower-case-service-id: true
  data:
    redis:
      host: redis
      port: 6379